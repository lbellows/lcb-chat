# Copy to .env and fill in. .env is gitignored — the token is a secret.
#
# Cloudflare Tunnel token for remote access (docker-compose.prod.yml).
# Get it from: Cloudflare Zero Trust > Networks > Tunnels > (your tunnel) >
# Install connector. It's the long string after `--token` in the shown command.
TUNNEL_TOKEN=
